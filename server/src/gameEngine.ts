import type { GameState, Player, Card } from "./../../shared/types";
  
export function shuffleDeck(deck: Card[]): Card[] {
  return deck;
}
  
export function startGame(game: GameState) {
  return game;
}